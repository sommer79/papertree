# papertree
A folder and reading layer for Paperless-ngx. Nested folders with inheritable filters, auto-generated subfolders, icons for folders and tags, logos for correspondents. Strictly read-only towards Paperless — it only ever writes its own tree. No build step, no browser dependencies
